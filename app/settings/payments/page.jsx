import SettingsShell from '../_SettingsShell';

export const metadata = { title: 'Payments & Billing — Vector' };

export default function PaymentsSettingsPage() {
  return <SettingsShell bucket="payments" />;
}
